# Printer-controlled scan mount

These are the original scanner-mount models used by the Marlin printer-controlled scanning workflow in `v3se_printer`.

- `scanner_mount.step`: editable solid model
- `scanner_mount.stl`: print-ready mesh
